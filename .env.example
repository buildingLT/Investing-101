# AI Advisor (get from console.anthropic.com)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Gmail Sync — URL of the running portfolio app (used by the cron script to POST imports)
# Leave as-is for local dev; update for production deployment
PORTFOLIO_URL=http://localhost:4000
